# 「生产曲线模板」部署手册

目标：把工具发布到公网，让任何人通过网址访问、上传数据、自动生成曲线并下载 Excel。

用到的两个免费平台：**GitHub**（存放代码）+ **Streamlit Community Cloud**（运行网页）。
整个流程大约 20 分钟，只需要一个 GitHub 账号。

---

## 第 0 步：注册 GitHub 账号

1. 打开 https://github.com 点击 **Sign up**；
2. 填邮箱、设置用户名和密码，完成注册（按页面提示验证邮箱即可）。

> 建议用户名用简单好记的英文，比如 `zhangsan2026`，后面网址会用到。

---

## 第 1 步：把项目文件夹变成 GitHub 仓库

### 方式 A：用 GitHub Desktop（图形界面，推荐新手）

1. 下载安装 GitHub Desktop：https://desktop.github.com ，安装后打开并用刚才的账号登录；
2. 菜单 **File → Add Local Repository**；
3. 选择桌面上的文件夹 **单井历史数据曲线图工具**（选到这一层即可）；
4. 弹窗询问“是否在此创建仓库？”点 **Create a Repository**；
5. 左侧列表会显示改动文件。**确认没有 `.venv` 文件夹**（已自动忽略，不要手动勾选它）；
6. 左下角填一句说明（比如 `first version`），点 **Commit to main**；
7. 点顶部 **Publish repository**，**不要勾选** “Keep this code private”（Streamlit 免费版要求公开仓库），点 **Publish**。

### 方式 B：用命令行

1. 在桌面「单井历史数据曲线图工具」文件夹里，按住 Shift 点右键，选择“在此处打开 PowerShell 窗口”；
2. 依次输入以下命令（每行回车一次）：

```powershell
git init
git add .
git commit -m "单井生产曲线模板 v1.0"
```

3. 打开 https://github.com/new ，仓库名填 `well-chart`，选 **Public**，直接点 **Create repository**；
4. 回到 PowerShell，按页面提示输入（把 `你的用户名` 换成自己的）：

```powershell
git remote add origin https://github.com/你的用户名/well-chart.git
git branch -M main
git push -u origin main
```

5. 第一次推送会弹出 GitHub 登录窗口，用浏览器登录授权即可。

---

## 第 2 步：在 Streamlit Community Cloud 上部署

1. 打开 https://share.streamlit.io ；
2. 点右上角 **Sign in**，选择 **Continue with GitHub**，授权登录；
3. 点 **New app**；
4. 按下面填：

   | 项目 | 填写内容 |
   | --- | --- |
   | Repository（仓库） | 选择 `你的用户名/well-chart` |
   | Branch（分支） | `main` |
   | Main file path（主文件） | `app.py` |

5. 点 **Deploy**，等待 2~5 分钟（第一次要安装依赖，稍慢）；
6. 部署完成自动生成网址，格式类似：

   `https://你的用户名-well-chart.streamlit.app`

把这个网址发给同事，任何人打开就能用。

---

## 第 3 步：以后怎么更新

改完代码后，把新版本推送到 GitHub，Streamlit 会自动重新部署（约 1~2 分钟）：

- GitHub Desktop：提交（Commit）→ 推送（Push origin）；
- 命令行：`git add .` → `git commit -m "说明"` → `git push`。

以后新增「日期叠合曲线模板」「天数叠合曲线模板」也是这样更新，不用重新部署。

---

## 常见问题

**Q：.venv 文件夹会上传吗？**
不会。项目里已配置忽略规则，`.venv`、`work`、`outputs` 都不会进入仓库；服务器会用 `requirements.txt` 重新安装所需组件。

**Q：上传的数据会泄露吗？**
网页是“用完即走”的：上传的文件只在当前访问者的浏览器会话里处理，不保存到服务器，也不写入数据库。但仓库代码是公开的（免费版要求），不含任何数据。

**Q：预览图上的中文会变成方块吗？**
不会。项目已内置免费中文字体（fonts 文件夹），服务器上也能正常显示中文。

**Q：部署失败怎么办？**
把 Streamlit 页面上的日志（Deploy → 展开日志）截图发过来，我可以帮你排查。

**Q：想用自己的域名？**
Streamlit 付费版支持自定义域名；也可以用“方式三：自己的服务器 + Nginx”实现（见 README）。
